# Pipelines Directory

## Purpose
This directory houses pipeline definition code. In this project, it contains DVC pipeline configurations and custom Python orchestrator scripts.

## Key Files
- `dvc.yaml` (symbolically linked or documented): Outlines stages like ingest -> validate -> preprocess -> train -> evaluate.
