apiVersion: v1
kind: Service
metadata:
  name: {{ include "customer-churn-app.fullname" . }}
  labels:
    {{- include "customer-churn-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "customer-churn-app.selectorLabels" . | nindent 4 }}
