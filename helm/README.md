# Helm Chart Directory

## Purpose
Contains Helm charts for packaging, configuring, and installing the customer churn application suite onto Kubernetes clusters in a repeatable way.

## Key Files
- `Chart.yaml`: Helm chart metadata.
- `values.yaml`: Customizable default values.
- `templates/`: Templates for Kubernetes service/deployment resources.
