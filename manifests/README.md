# Kubernetes Manifests Directory

## Purpose
Contains the raw YAML manifest files for deploying applications and monitoring agents to local or remote Kubernetes clusters.

## Key Files
- `deployment.yaml`: Defines deployment parameters for the API service.
- `service.yaml`: Configures internal networking.
- `ingress.yaml`: Exposes the endpoint externally.
- `namespace.yaml`: Standard isolated namespace for deployment.
