# Notebooks Directory

## Purpose
This directory contains Jupyter notebooks used for exploratory data analysis (EDA), prototype model training, and drift detection testing.

## Guidelines
- Notebooks are for experimentation only. Production code must be modularized and moved to `src/`.
- Run clean-up on notebook cells before committing to avoid committing large binary outputs.
