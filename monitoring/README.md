# Monitoring Directory

## Purpose
This directory holds the configuration files and dashboards for Prometheus metrics collection and Grafana data visualization.

## Key Files
- `prometheus.yaml`: Configures the scraping intervals and targets (e.g., FastAPI metrics endpoints).
- `dashboards/`: Directory holding JSON configurations for Grafana dashboards (system health and model drift dashboard).
